{
  "name": "agentic-code-reviewer-skill",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "build":       "bun run --cwd packages/client build",
    "dev":         "bun run --cwd packages/server dev & bun run --cwd packages/client dev",
    "start":       "bun packages/server/src/index.ts",
    "test":        "bun test tests/",
    "test:unit":   "bun test tests/unit/",
    "test:parity": "bun test tests/parity/",
    "build:vscode": "bun run --cwd apps/vscode-extension build",
    "dev:vscode": "bun run --cwd apps/vscode-extension watch",
    "test:vscode": "bun run --cwd apps/vscode-extension test",
    "package:vscode": "bun run --cwd apps/vscode-extension package",
    "publish:vscode": "bun run --cwd apps/vscode-extension package && bun run --cwd apps/vscode-extension vsce-publish",
    "compile": "bun run build && bun build packages/server/src/index.ts --compile --outfile dist/review-server"
  }
}
