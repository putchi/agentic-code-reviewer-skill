#!/bin/sh
# Agentic Code Reviewer WebView Router
# Invoked via ACR_BROWSER env var.

url="$1"
[ -z "$url" ] && exit 0
[ -z "$ACR_VSCODE_PORT" ] && exit 1

curl -s -G "http://127.0.0.1:${ACR_VSCODE_PORT}/open" \
  --data-urlencode "url=${url}" >/dev/null 2>&1
