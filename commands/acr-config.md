---
name: acr-config
description: Configure the Agentic Code Reviewer for this repo — create or update .acr.json (pause/disable the review gate, set models, exclude files from review).
---

Invoke the acr-config skill and follow it exactly.

User request: $ARGUMENTS

If no arguments were given, ask what they want to configure (pause reviews, exclude paths, change models) or infer it from recent conversation context.
